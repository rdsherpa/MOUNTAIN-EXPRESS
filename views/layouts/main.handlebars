<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mountain Express</title>
    <link rel="stylesheet" href="./stylesheets/style.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com"><link rel="preconnect" href="https://fonts.gstatic.com" crossorigin><link href="https://fonts.googleapis.com/css2?family=Merriweather:ital,wght@0,400;0,700;1,400&display=swap" rel="stylesheet">
  </head>

<body>
  <header>
    <nav class="flex justify-between items-center h-10 items-center bg-sky-800 lg:h-20">
        <div class="pl-2">
          <a href="/">
            <img
            src="/images/logo1.png"
            alt=""
            class="w-[35%] max-w-[35%] md:w-2/4 md:max-w-[50%] h-[100%]"
          </a>
        </div>

        <div class="pr-4">
    {{!-- if you are able to log in then you are given a chance to logout as well --}} 
          {{#if logged_in}}
            <button class="btn" id="logout">logout</button>
          {{else}}
            <button class="text-white bg-amber-500 p-1 lg:p-2 rounded-md px-4 lg:rounded-2xl mt-2 hover:bg-amber-200 text-[0.6rem] lg:text-lg hover:text-white mb-2 w-[100px] md:w-[200px]">
            <a href="/login">Login/Sign-up</a>           
            </button>
            </a>
          {{/if}}
        </div>
      </nav>
  </header>
  <main>
    {{{ body }}}
  </main>

<footer class="flex items-center justify-center h-40 pt-3 flex-col">
      <h5>Footer</h5>
      <ul class="flex gap-3">
        <li>Home</li>
        <li><a href="/booking">Book</a></li>
        <li>About</li>
        <li>Contact</li>
      </ul>
    </footer>

  {{#if logged_in}}
  <script src="javascript/logout.js"></script>
  {{/if}}
{{!-- <script src="/javascript/main.js"></script> --}}
</body>
</html>